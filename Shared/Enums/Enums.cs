namespace Shared.Enums
{
    public enum Status
    {
        Planned,
        InProgress,
        Finished,
        Abandoned
    }

    public enum Category
    {
        Book,
        Movie,
        Game
    }

    public enum Rating
    {
        S, A, B, C, D, F
    }
}
